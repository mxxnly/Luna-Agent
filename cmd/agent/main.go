package main

import (
	"fmt"
	"os"
)

// Placeholder until Milestone 1 (enroll + heartbeat).
func main() {
	fmt.Fprintln(os.Stderr, "lunaagentd: not implemented yet (see docs/architecture.md)")
	os.Exit(1)
}
